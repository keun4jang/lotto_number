name: Weekly Lotto Recommendation

on:
  schedule:
    # 로또 추첨일: 토요일 → 전날(금요일)에 09:00, 18:00 KST 하루 2회 발송
    - cron: "0 0 * * 5"  # 금요일 09:00 KST
    - cron: "0 9 * * 5"  # 금요일 18:00 KST
  workflow_dispatch:

jobs:
  recommend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main (latest self-tuned config)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        working-directory: lotto-doctor
        run: pip install -e .

      - name: Collect latest draw data
        working-directory: lotto-doctor
        run: lotto-doctor collect --source github

      - name: Generate and send recommendation (force-refresh with latest config)
        working-directory: lotto-doctor
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: lotto-doctor recommend --send --force

      - name: Commit DB and reports
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -f lotto-doctor/data/lotto.db
          git add lotto-doctor/reports/
          if git diff --cached --quiet; then
            echo "변경 없음, 커밋 건너뜀."
          else
            git commit -m "chore: weekly recommendation $(date -u +%Y-%m-%d)"
            git pull --rebase origin main
            git push origin main
          fi
